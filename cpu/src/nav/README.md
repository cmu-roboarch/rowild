This directory includes the implementation of navigation kernels.

`astar-bare/` includes an abstract implementation of the A\* algorithm. The
other navigation kernels use this abstract implementation for planning.

`car/` includes the implementation of a car navigation scenario.

`drone/` includes the implementation of a drone navigation scenario.

`legged/` includes the implementation of the navigation of a legged robot.
